---
interface Props {
  text: string;
}

const { text } = Astro.props;
---

<h2 class="font-[family-name:var(--font-heading)] text-3xl md:text-4xl font-bold neon-underline mb-12 text-center">
  {text}
</h2>
