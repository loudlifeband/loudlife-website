---
interface Props {
  text: string;
  tag?: 'h1' | 'h2' | 'h3' | 'span';
  class?: string;
}

const { text, tag: Tag = 'h1', class: className = '' } = Astro.props;
---

<Tag class:list={['glitch', className]} data-text={text}>
  {text}
</Tag>
